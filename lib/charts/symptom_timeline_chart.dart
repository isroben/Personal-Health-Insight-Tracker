/// ==========================================================================
/// symptom_timeline_chart.dart
/// ==========================================================================
/// Placeholder for fl_chart timeline
import 'package:flutter/material.dart';
class SymptomTimelineChart extends StatelessWidget {
  const SymptomTimelineChart({super.key});
  @override
  Widget build(BuildContext context) => const Placeholder(fallbackHeight: 200);
}
