/// ==========================================================================
/// weekly_summary_chart.dart
/// ==========================================================================
/// Placeholder for fl_chart line chart
import 'package:flutter/material.dart';
class WeeklySummaryChart extends StatelessWidget {
  const WeeklySummaryChart({super.key});
  @override
  Widget build(BuildContext context) => const Placeholder(fallbackHeight: 200);
}
