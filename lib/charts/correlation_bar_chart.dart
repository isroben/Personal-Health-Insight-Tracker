/// ==========================================================================
/// correlation_bar_chart.dart
/// ==========================================================================
/// Placeholder for fl_chart bar chart
import 'package:flutter/material.dart';
class CorrelationBarChart extends StatelessWidget {
  const CorrelationBarChart({super.key});
  @override
  Widget build(BuildContext context) => const Placeholder(fallbackHeight: 200);
}
